{
  "name": "apex-capital-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start"
  },
  "dependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.61.1"
  }
}
